services:
  - type: web
    name: whisper-transcription
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python whisper_service.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0